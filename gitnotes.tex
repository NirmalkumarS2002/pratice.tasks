1. git
2. git --version
3. git config --global user.name "NirmalkumarS2002"
4. git config --global user.email "nirmalsiva555@gmail.com"
5. git config --list 

6. git clone https://github.com/NirmalkumarS2002/pratice.tasks.git
7. cd folder name
8. git status

9. git add sign.html (its moving ot staging area)
10 git commint -m "sign page added" (its movedto git )
11 git push origin main (file moved to git repp)

12 git pull (rep la chagnge pannatha working area ku kondu varum)

13 git init (it will create empty reppo)
14 git remote add origin  origin https://github.com/NirmalkumarS2002/pratice.task2.git  (it will connect reppo and local fil)